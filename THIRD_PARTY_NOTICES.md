# Third-party notices

ProteusLab SDL dynamically links to SDL2. SDL2 is distributed under the zlib license. The project does not copy SDL2 source code into this repository; the Windows setup script installs the official MSYS2 package.

The schematic renderer, bitmap font, JSON parser and PNG writer in this project are original project code and do not add third-party runtime dependencies.

